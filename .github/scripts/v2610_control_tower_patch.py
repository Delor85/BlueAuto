from pathlib import Path


def replace(path, old, new, count=1):
    p=Path(path); s=p.read_text(encoding='utf-8')
    if old not in s: raise SystemExit(f'anchor missing in {path}: {old[:90]}')
    p.write_text(s.replace(old,new,count),encoding='utf-8')

# Version identity.
replace('app/build.gradle','versionCode 49','versionCode 50')
replace('app/build.gradle','versionName "2.6.9"','versionName "2.6.10"')
replace('app/src/main/java/com/profitloop/blueauto/ApiClient.java','payload.put("app_version", "2.6.8");','payload.put("app_version", "2.6.10");',2)
replace('cloudflare/src/index.js',"const API_VERSION = '2.6.8-cloudflare';","const API_VERSION = '2.6.10-cloudflare';")

# Worker: parent may override the rate for this single proactive SUPPLY_CHILD only.
replace('cloudflare/src/index.js',"""    const value = amount(input.amount);\n    const policy = await commissionRateFor(env, requester, child.node_code);\n    const quote = commissionQuote(value, policy.rate_bps);\n    return {\n      executor: requester, executorPhone: auth.phone_number,\n      targetNode: child.node_code, targetPhone: child.phone_number, amount: quote.total_amount,\n      requestedBaseAmount: value, commissionRateBps: policy.rate_bps,\n      commissionAmount: quote.commission_amount, commissionSource: policy.source,""", """    const value = amount(input.amount);\n    let policy = await commissionRateFor(env, requester, child.node_code);\n    if (input.commission_rate_bps !== undefined && input.commission_rate_bps !== null && String(input.commission_rate_bps) !== '') {\n      const oneOffRate = Number(input.commission_rate_bps);\n      if (!Number.isInteger(oneOffRate) || oneOffRate < 0 || oneOffRate > 5000) {\n        throw new ApiError('INVALID_COMMISSION_RATE', 'Le taux de commission de cet envoi doit être compris entre 0 et 50 %.', 422);\n      }\n      policy = {rate_bps: oneOffRate, source: 'TRANSACTION_OVERRIDE'};\n    }\n    const quote = commissionQuote(value, policy.rate_bps);\n    return {\n      executor: requester, executorPhone: auth.phone_number,\n      targetNode: child.node_code, targetPhone: child.phone_number, amount: quote.total_amount,\n      requestedBaseAmount: value, commissionRateBps: policy.rate_bps,\n      commissionAmount: quote.commission_amount, commissionSource: policy.source,""")

# Android bridge supports a rate protected by a fresh preview/fingerprint, plus explicit sync.
p=Path('app/src/main/java/com/profitloop/blueauto/MainActivity.java'); s=p.read_text(encoding='utf-8')
s=s.replace('import android.webkit.JsResult;','import android.webkit.JsResult;\nimport android.webkit.JsPromptResult;')
s=s.replace("""            public boolean onJsConfirm(WebView view, String url, String message, JsResult result) {\n                new AlertDialog.Builder(MainActivity.this)\n                        .setTitle(\"Confirmation Blue Magic\")\n                        .setMessage(message)\n                        .setPositiveButton(\"CONFIRMER\", (dialog, which) -> result.confirm())\n                        .setNegativeButton(\"ANNULER\", (dialog, which) -> result.cancel())\n                        .setOnCancelListener(dialog -> result.cancel())\n                        .show();\n                return true;\n            }""", """            public boolean onJsConfirm(WebView view, String url, String message, JsResult result) {\n                new AlertDialog.Builder(MainActivity.this)\n                        .setTitle(\"Confirmation Blue Magic\")\n                        .setMessage(message)\n                        .setPositiveButton(\"CONFIRMER\", (dialog, which) -> result.confirm())\n                        .setNegativeButton(\"ANNULER\", (dialog, which) -> result.cancel())\n                        .setOnCancelListener(dialog -> result.cancel())\n                        .show();\n                return true;\n            }\n\n            @Override\n            public boolean onJsPrompt(WebView view, String url, String message, String defaultValue, JsPromptResult result) {\n                final EditText input = new EditText(MainActivity.this);\n                input.setInputType(InputType.TYPE_CLASS_NUMBER | InputType.TYPE_NUMBER_FLAG_DECIMAL);\n                input.setText(defaultValue == null ? \"\" : defaultValue);\n                input.setSelectAllOnFocus(true);\n                int pad = dp(18); input.setPadding(pad, dp(8), pad, dp(8));\n                new AlertDialog.Builder(MainActivity.this)\n                        .setTitle(\"Taux de commission\")\n                        .setMessage(message)\n                        .setView(input)\n                        .setPositiveButton(\"VALIDER LE TAUX\", (dialog, which) -> result.confirm(input.getText().toString().trim()))\n                        .setNegativeButton(\"ANNULER\", (dialog, which) -> result.cancel())\n                        .setOnCancelListener(dialog -> result.cancel())\n                        .show();\n                return true;\n            }""")
# Reduce native Gérer overload: these actions are now direct control-tower actions.
s=s.replace('        addManagementPair(tools, verifySim, repairPairing);','        addManagementFull(tools, repairPairing);')
s=s.replace('            addManagementPair(tools, prepare, battery);\n            toggle = managementActionButton(AppConfig.robotEnabled(this)\n                    ? "■ ARRÊTER ROBOT" : "▶ DÉMARRER ROBOT", VIOLET);\n            addManagementFull(tools, toggle);','            // Autorisations, batterie et démarrage sont désormais visibles directement dans la Tour de contrôle.\n            toggle = managementActionButton(AppConfig.robotEnabled(this)\n                    ? "■ ARRÊTER ROBOT" : "▶ DÉMARRER ROBOT", VIOLET);')
s=s.replace('                value.put("accessibility_enabled", BlueAccessibilityService.isEnabled(MainActivity.this));','                value.put("accessibility_enabled", BlueAccessibilityService.isEnabled(MainActivity.this));\n                value.put("accessibility_connected", BlueAccessibilityService.isConnected());')
# Add rate-aware bridge helpers before createCommand.
anchor='''        @JavascriptInterface\n        public void createCommand(String requestType, String targetNode, String targetPhone,'''
insert='''        @JavascriptInterface\n        public void previewCommandWithCommissionRate(String requestType, String targetNode, String targetPhone,\n                                                     String amount, String clientRequestId, String capacityCheckId,\n                                                     String commandArgument, int commissionRateBps) {\n            new Thread(() -> {\n                try {\n                    JSONObject payload = new JSONObject();\n                    payload.put("request_type", requestType == null ? "" : requestType);\n                    payload.put("target_node_code", targetNode == null ? "" : targetNode.trim().toUpperCase());\n                    payload.put("target_phone", targetPhone == null ? "" : targetPhone.trim());\n                    payload.put("amount", amount == null ? "" : amount.trim());\n                    payload.put("client_request_id", validRequestId(clientRequestId));\n                    payload.put("capacity_check_id", capacityCheckId == null ? "" : capacityCheckId.trim());\n                    payload.put("transaction_id", commandArgument == null ? "" : commandArgument.trim());\n                    payload.put("commission_rate_bps", commissionRateBps);\n                    callback("onCommandPreview", new ApiClient(MainActivity.this).previewCommand(payload));\n                } catch (Exception error) { callbackError("onCommandPreview", error); }\n            }).start();\n        }\n\n'''+anchor
if anchor not in s: raise SystemExit('MainActivity createCommand anchor missing')
s=s.replace(anchor,insert,1)
# Add one-off rate create + sync before checkPurchaseCapacity.
anchor='''        @JavascriptInterface\n        public void checkPurchaseCapacity(String amount, String clientRequestId) {'''
insert='''        @JavascriptInterface\n        public void createCommandWithCommissionRate(String requestType, String targetNode, String targetPhone,\n                                                     String amount, String clientRequestId,\n                                                     String confirmationFingerprint, String capacityCheckId,\n                                                     String commandArgument, int commissionRateBps) {\n            new Thread(() -> {\n                try {\n                    JSONObject payload = new JSONObject();\n                    payload.put("request_type", requestType == null ? "" : requestType);\n                    payload.put("target_node_code", targetNode == null ? "" : targetNode.trim().toUpperCase());\n                    payload.put("target_phone", targetPhone == null ? "" : targetPhone.trim());\n                    payload.put("amount", amount == null ? "" : amount.trim());\n                    payload.put("client_request_id", validRequestId(clientRequestId));\n                    payload.put("confirmation_fingerprint", confirmationFingerprint == null ? "" : confirmationFingerprint.trim().toLowerCase(Locale.ROOT));\n                    payload.put("capacity_check_id", capacityCheckId == null ? "" : capacityCheckId.trim());\n                    payload.put("transaction_id", commandArgument == null ? "" : commandArgument.trim());\n                    payload.put("commission_rate_bps", commissionRateBps);\n                    callback("onCommandCreated", new ApiClient(MainActivity.this).createCommand(payload));\n                    if (AppConfig.anyRobotEnabled(MainActivity.this)) RobotService.startEnabled(MainActivity.this);\n                } catch (Exception error) { callbackError("onCommandCreated", error); }\n            }).start();\n        }\n\n        @JavascriptInterface\n        public void synchronizeNow() {\n            RobotService.forceSync(MainActivity.this);\n            runOnUiThread(() -> toast("Synchronisation de tous les comptes relancée immédiatement."));\n        }\n\n'''+anchor
if anchor not in s: raise SystemExit('MainActivity capacity anchor missing')
s=s.replace(anchor,insert,1)
p.write_text(s,encoding='utf-8')

# Accessibility liveness is distinguishable from permission state; app never self-grants it.
p=Path('app/src/main/java/com/profitloop/blueauto/BlueAccessibilityService.java'); s=p.read_text(encoding='utf-8')
s=s.replace('''    static boolean isEnabled(Context context) {''','''    static boolean isConnected() { return liveInstance != null; }\n\n    @Override\n    public void onInterrupt() {\n        if (liveInstance == this) liveInstance = null;\n    }\n\n    @Override\n    public void onDestroy() {\n        if (liveInstance == this) liveInstance = null;\n        handler.removeCallbacksAndMessages(null);\n        super.onDestroy();\n    }\n\n    static boolean isEnabled(Context context) {''',1)
p.write_text(s,encoding='utf-8')

# Robot sync: explicit wake and bounded multi-profile final-report recovery.
p=Path('app/src/main/java/com/profitloop/blueauto/RobotService.java'); s=p.read_text(encoding='utf-8')
s=s.replace('static final String ACTION_AUDIT = "com.profitloop.blueauto.REQUEST_AUDIT";','static final String ACTION_AUDIT = "com.profitloop.blueauto.REQUEST_AUDIT";\n    static final String ACTION_FORCE_SYNC = "com.profitloop.blueauto.FORCE_SYNC";')
s=s.replace('''        if (ACTION_AUDIT.equals(action)) {''','''        if (ACTION_FORCE_SYNC.equals(action)) {\n            apiRetryAtByProfile.clear();\n            apiFailureCountByProfile.clear();\n            lastHeartbeatByProfile.clear();\n            backoffMs = 250L;\n            updateNotification(robotSummary("Synchronisation forcée de tous les comptes"));\n            scheduleCycle(0L);\n            return START_STICKY;\n        }\n\n        if (ACTION_AUDIT.equals(action)) {''',1)
s=s.replace('boolean reportOutstanding = retryOneDueFinalReport();','boolean reportOutstanding = retryDueFinalReports(4);')
s=s.replace('''    private boolean retryOneDueFinalReport() {\n        boolean outstanding = false;\n        boolean attempted = false;\n        for (String profileId : AppConfig.profileIds(this)) {\n            JSONObject pending = PendingCommandStore.get(this, profileId);\n            if (pending == null || !PendingCommandStore.REPORT_PENDING.equals(\n                    pending.optString("local_state", ""))) continue;\n            if (!attempted && PendingCommandStore.isFinalReportRetryDue(pending)) {\n                retryFinalReport(profileId, pending);\n                attempted = true;\n                pending = PendingCommandStore.get(this, profileId);\n            }\n            if (pending != null && PendingCommandStore.REPORT_PENDING.equals(\n                    pending.optString("local_state", ""))) outstanding = true;\n        }\n        return outstanding;\n    }''','''    private boolean retryDueFinalReports(int maxAttempts) {\n        boolean outstanding = false;\n        int attempted = 0;\n        for (String profileId : AppConfig.profileIds(this)) {\n            JSONObject pending = PendingCommandStore.get(this, profileId);\n            if (pending == null || !PendingCommandStore.REPORT_PENDING.equals(\n                    pending.optString("local_state", ""))) continue;\n            if (attempted < Math.max(1, maxAttempts) && PendingCommandStore.isFinalReportRetryDue(pending)) {\n                retryFinalReport(profileId, pending);\n                attempted += 1;\n                pending = PendingCommandStore.get(this, profileId);\n            }\n            if (pending != null && PendingCommandStore.REPORT_PENDING.equals(\n                    pending.optString("local_state", ""))) outstanding = true;\n        }\n        return outstanding;\n    }''')
s=s.replace('''    static void startEnabled(Context context) {\n        sendServiceAction(context, new Intent(context, RobotService.class).setAction(ACTION_WAKE));\n    }''','''    static void startEnabled(Context context) {\n        sendServiceAction(context, new Intent(context, RobotService.class).setAction(ACTION_WAKE));\n    }\n\n    static void forceSync(Context context) {\n        sendServiceAction(context, new Intent(context, RobotService.class).setAction(ACTION_FORCE_SYNC));\n    }''')
p.write_text(s,encoding='utf-8')

# Financial confirmation semantics: requester complains to superior; proactive parent chooses/validates the rate.
p=Path('app/src/main/assets/app.js'); s=p.read_text(encoding='utf-8')
s=s.replace('''        if(!confirmFinancial(preview)){setBusy(false);pendingPreview=null;submittedForm=null;showToast('Commande annulée avant toute composition.');return;}\n        showActionProgress('Commande confirmée. Transmission au Robot de la SIM fournisseur…');\n        window.AndroidBridge.createCommand(pendingPreview.type,pendingPreview.node,pendingPreview.phone,\n            pendingPreview.amount,pendingPreview.request_id,fingerprint,pendingPreview.capacity_check_id,pendingPreview.argument);''','''        var decision=confirmFinancial(preview);\n        if(decision==='REPREVIEW'){return;}\n        if(!decision){setBusy(false);pendingPreview=null;submittedForm=null;showToast('Commande annulée avant toute composition.');return;}\n        showActionProgress('Commande confirmée. Transmission au Robot de la SIM fournisseur…');\n        if(pendingPreview.type==='SUPPLY_CHILD'&&typeof pendingPreview.commission_rate_bps==='number'&&window.AndroidBridge.createCommandWithCommissionRate){\n            window.AndroidBridge.createCommandWithCommissionRate(pendingPreview.type,pendingPreview.node,pendingPreview.phone,\n                pendingPreview.amount,pendingPreview.request_id,fingerprint,pendingPreview.capacity_check_id,pendingPreview.argument,pendingPreview.commission_rate_bps);\n        }else{\n            window.AndroidBridge.createCommand(pendingPreview.type,pendingPreview.node,pendingPreview.phone,\n                pendingPreview.amount,pendingPreview.request_id,fingerprint,pendingPreview.capacity_check_id,pendingPreview.argument);\n        }''')
start=s.index('    function confirmFinancial(preview){')
end=s.index('    function formatMoney(value)',start)
new_func='''    function confirmFinancial(preview){\n        if(preview.dangerous){\n            return window.confirm('COMMANDE CAMTEL SENSIBLE\\n\\n'+(preview.operation_label||preview.operation||'Maintenance SIM')+'\\n\\nCOMPTE EXÉCUTEUR : '+(preview.executor_node_code||'—')+' — '+(preview.executor_phone||'—')+'\\nCIBLE : '+(preview.target_node_code||preview.executor_node_code||'—')+' — '+(preview.target_phone||preview.executor_phone||'—')+'\\n\\nCette commande peut suspendre ou geler une SIM. Vérifiez la cible avant de confirmer. Le PIN reste uniquement sur le Robot.\\n\\nCONFIRMER CETTE COMMANDE ?');\n        }\n        var title=preview.operation==='RETAIL_TRANSFER'?'VENTE À UN CLIENT':'TRANSFERT DE CRÉDIT';\n        var supplier=(preview.executor_node_code||'FOURNISSEUR')+' — '+(preview.executor_phone||'numéro absent');\n        var beneficiary=(preview.target_node_code||'CLIENT')+' — '+(preview.target_phone||'numéro absent');\n        var base=Number(preview.requested_base_amount||preview.amount||0), commission=Number(preview.commission_amount||0), rateBps=Number(preview.commission_rate_bps||0), rate=rateBps/100;\n        var quote=preview.operation==='DISTRIBUTION_TRANSFER' ? ('\\nMONTANT DE BASE : '+formatMoney(base)+' FCFA\\nCOMMISSION ('+String(rate).replace('.',',')+' %) : '+formatMoney(commission)+' FCFA\\nTOTAL À RECEVOIR : '+formatMoney(preview.amount)+' FCFA') : ('\\nMONTANT : '+formatMoney(preview.amount)+' FCFA');\n        if(pendingPreview&&pendingPreview.type==='SUPPLY_CHILD'){\n            var entered=window.prompt('Vous êtes le supérieur de ce bénéficiaire. Vérifiez ou modifiez le taux appliqué uniquement à cet envoi (0 à 50 %). Le taux par défaut/personnalisé du compte n’est pas modifié par ce choix.',String(rate).replace('.',','));\n            if(entered===null){return false;}\n            var normalized=String(entered).replace(',','.').replace(/^\\s+|\\s+$/g,''), chosen=Number(normalized), chosenBps=Math.round(chosen*100);\n            if(!isFinite(chosen)||chosen<0||chosen>50){showToast('Taux invalide : choisissez une valeur entre 0 et 50 %.');return false;}\n            if(chosenBps!==rateBps){\n                pendingPreview.commission_rate_bps=chosenBps;\n                showActionProgress('Nouveau taux '+String(chosen).replace('.',',')+' % : recalcul et nouvelle certification serveur…');\n                if(window.AndroidBridge.previewCommandWithCommissionRate){\n                    window.AndroidBridge.previewCommandWithCommissionRate(pendingPreview.type,pendingPreview.node,pendingPreview.phone,pendingPreview.amount,pendingPreview.request_id,pendingPreview.capacity_check_id,pendingPreview.argument,chosenBps);\n                    return 'REPREVIEW';\n                }\n                showToast('Cette version Android ne sait pas recertifier un taux ponctuel.');return false;\n            }\n            pendingPreview.commission_rate_bps=rateBps;\n            return window.confirm('CONFIRMATION 1 SUR 2\\n\\n'+title+'\\n\\nFOURNISSEUR : '+supplier+'\\nBÉNÉFICIAIRE : '+beneficiary+quote+'\\n\\nVOUS ÊTES LE SUPÉRIEUR : ce taux est celui que vous validez pour cet envoi. Après confirmation, le Robot composera exactement le total certifié et contrôlera le pop-up Camtel avant le PIN.\\n\\nCONFIRMER CETTE TRANSACTION ?');\n        }\n        var guidance=pendingPreview&&pendingPreview.type==='REQUEST_SUPPLY' ? 'Le taux est fixé par votre supérieur. S’il ne vous convient pas, annulez maintenant et contactez directement votre supérieur avant de commander.' : 'Après confirmation, Blue Magic contrôlera les mêmes données dans le pop-up Camtel avant d’insérer le PIN.';\n        return window.confirm('CONFIRMATION 1 SUR 2\\n\\n'+title+'\\n\\nFOURNISSEUR : '+supplier+'\\nBÉNÉFICIAIRE : '+beneficiary+quote+'\\n\\n'+guidance+'\\n\\nCONFIRMER CETTE TRANSACTION ?');\n    }\n'''
s=s[:start]+new_func+s[end:]
p.write_text(s,encoding='utf-8')

# Load the additive control-tower layer last.
p=Path('app/src/main/assets/index.html'); s=p.read_text(encoding='utf-8')
s=s.replace('<link rel="stylesheet" href="style.css">','<link rel="stylesheet" href="style.css">\n    <link rel="stylesheet" href="control-tower-v2610.css">')
s=s.replace('<script src="platform-v267.js"></script>','<script src="platform-v267.js"></script>\n  <script src="control-tower-v2610.js"></script>')
p.write_text(s,encoding='utf-8')

# Control-tower CSS: reference-inspired density, high contrast, persistent dock.
Path('app/src/main/assets/control-tower-v2610.css').write_text(r'''/* B.I.R. v2.6.10 — Tour de contrôle additive. */
.form-editing .module-tabs{display:flex!important}.form-editing main{padding-bottom:112px!important}
.bir-balance-card.bir-control-balance{margin:2px 0 8px;border:1px solid rgba(78,225,255,.72);box-shadow:0 0 20px rgba(38,112,255,.28),0 8px 26px rgba(0,0,0,.25)}
.hero{background:linear-gradient(120deg,#f8fbff,#eaf5ff)!important;color:#08224a!important;border-color:rgba(38,112,255,.25)!important;padding:10px 12px!important;min-height:88px}.hero .eyebrow{color:#087eae!important}.hero .muted{color:#395774!important;font-weight:700}.hero h2{color:#081f45!important;font-size:1.45rem!important}.status-orb{min-width:112px;color:#087846!important;background:#e9fff5!important;border-color:#6fd4a7!important;cursor:pointer;padding:9px!important}.status-orb.robot-off{color:#9a5a00!important;background:#fff4d4!important;border-color:#f2be4b!important}.status-orb small{display:block;font-size:.55rem;margin-top:4px;color:inherit;font-weight:700}
.notice.notice-danger.bir-actionable{cursor:pointer;border-width:2px;padding:11px 13px}.notice.notice-danger.bir-actionable:after{content:'Touchez ici pour corriger maintenant';display:block;margin-top:5px;font-size:.68rem;font-weight:900;color:#8c172a}
.bir-quick-controls{display:flex;gap:6px;margin:7px 0 9px}.bir-quick-controls button{flex:1;min-width:0;margin:0;min-height:57px;padding:6px 3px;font-size:.67rem;background:#fff;color:#08224a;border:1px solid #c6ddf6;box-shadow:0 5px 15px rgba(0,30,80,.12)}.bir-quick-controls button b{display:block;font-size:1.05rem;margin-bottom:2px}.bir-quick-controls button.attention{background:linear-gradient(135deg,#fff2bd,#ffd25f);border-color:#efb62b}.bir-quick-controls button.good{background:linear-gradient(135deg,#e9fff5,#c8f5df)}
.bir-commission-control,.bir-remote-result{background:#fff!important;color:#08224a!important;border:1px solid #cbe1f8!important;padding:13px!important;margin:8px 0!important}.bir-commission-control .eyebrow,.bir-remote-result .eyebrow{color:#087eae!important}.bir-commission-control h3,.bir-remote-result h3{margin:4px 0 7px;color:#08224a}.bir-commission-control p,.bir-remote-result p{color:#395774}.bir-commission-control input{background:#f6fbff;color:#08224a;border-color:#76c8e9}.bir-commission-control .ct-row{display:flex;gap:7px}.bir-commission-control .ct-row>*{flex:1;min-width:0}.bir-commission-control button{background:linear-gradient(105deg,#078fd4,#235bd0)}.bir-rate-list{margin-top:8px;padding:8px;background:#edf7ff;border-radius:9px;font-size:.75rem;line-height:1.45}
.bir-remote-result strong{display:block;font-size:1.08rem;color:#087846;margin:5px 0}.bir-remote-result .result-body{white-space:pre-wrap;font-size:.84rem;line-height:1.45;color:#142d4d}
.bir-bottom-nav{visibility:visible!important;opacity:1!important}.form-editing .bir-bottom-nav{opacity:.96!important}
@media(max-width:360px){.bir-quick-controls{gap:4px}.bir-quick-controls button{font-size:.59rem;min-height:53px}.hero{min-height:78px}.status-orb{min-width:98px}.bir-commission-control .ct-row{display:block}.bir-balance-card.bir-control-balance{margin-top:0}}
''',encoding='utf-8')

# ES5 additive UI, compatible with Android 6 WebView.
Path('app/src/main/assets/control-tower-v2610.js').write_text(r'''(function(){
'use strict';
function id(x){return document.getElementById(x);} function cfg(){try{return JSON.parse(window.AndroidBridge.getConfiguration()||'{}');}catch(e){return {};}}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function official(code,role,parent){code=String(code||'').toUpperCase();parent=String(parent||'').toUpperCase();if(role==='DSM'&&/^DSM\d+$/.test(code)&&parent){return code+'_'+parent;}if(role==='POS'&&/^POS\d+$/.test(code)&&parent){return code+'_'+parent;}return code;}
function platform(action,payload){if(window.AndroidBridge&&window.AndroidBridge.platformAction){window.AndroidBridge.platformAction(action,JSON.stringify(payload||{}));}}
function boot(){var c=cfg(),main=document.querySelector('main'),hero=document.querySelector('.hero'),balance=document.querySelector('.bir-balance-card'),off=!c.robot_enabled,simBad=c.mode==='ROBOT'&&!c.sim_verified;
 if(balance&&main&&hero){balance.className+=' bir-control-balance';main.insertBefore(balance,hero);}
 var shown=official(c.node_code,String(c.role||'').toUpperCase(),c.parent_node_code);if(shown){if(id('nodeCode'))id('nodeCode').textContent=shown;if(id('birAccountNode'))id('birAccountNode').textContent=shown;}
 var state=id('robotState');if(state){state.className='status-orb'+(off?' robot-off':'');state.innerHTML=(off?'ROBOT ARRÊTÉ':'ROBOT ACTIF')+(off?'<small>Toucher pour démarrer</small>':'<small>Opérationnel</small>');state.onclick=function(){if(!c.robot_enabled&&window.AndroidBridge.startRobot){window.AndroidBridge.startRobot();}};}
 var fatal=id('fatalNotice');if(fatal&&simBad){fatal.className='notice notice-danger bir-actionable';fatal.textContent='SIM NON VÉRIFIÉE : le Robot ne peut pas prendre de commande financière.';fatal.onclick=function(){window.AndroidBridge.verifySim();};}
 var quick=document.createElement('div');quick.className='bir-quick-controls';quick.innerHTML='<button id="ctPerm"><b>✓</b>AUTORISATIONS</button><button id="ctRobot"><b>∞</b>ROBOT</button><button id="ctBattery"><b>▣</b>BATTERIE</button><button id="ctSync"><b>↻</b>SYNCHRONISER</button>';if(hero&&hero.parentNode){hero.parentNode.insertBefore(quick,hero.nextSibling);}
 id('ctPerm').onclick=function(){window.AndroidBridge.prepareRobotPermissions();};id('ctRobot').onclick=function(){if(!c.robot_enabled)window.AndroidBridge.startRobot();};id('ctBattery').onclick=function(){window.AndroidBridge.openBatteryOptimizationSettings();};id('ctSync').onclick=function(){if(window.AndroidBridge.synchronizeNow)window.AndroidBridge.synchronizeNow();if(window.AndroidBridge.loadDashboard)window.setTimeout(function(){window.AndroidBridge.loadDashboard();},800);};
 if(c.accessibility_enabled){id('ctPerm').className='good';}if(c.robot_enabled){id('ctRobot').className='good';}else{id('ctRobot').className='attention';}
 roleFields(c);resultCard();commissionCard(c);hideDuplicates();wrapCallbacks(c);
}
function roleFields(c){var r=String(c.role||'').toUpperCase(),label=id('childNode')&&id('childNode').previousElementSibling,b=id('balanceChildNode'),a=id('adminChildNode'),req=document.querySelector('#requestSupplyCard p.muted');if(r==='DAE'){if(label)label.textContent='Code du DSM direct';if(id('childNode'))id('childNode').placeholder='Ex. DSM1_SU1 ou DSM1';if(b){b.placeholder='Ex. DSM1_SU1 ou DSM1';if(b.previousElementSibling)b.previousElementSibling.textContent='DSM direct';}if(a){a.placeholder='Ex. DSM1_SU1 ou DSM1';if(a.previousElementSibling)a.previousElementSibling.textContent='DSM direct';}}else if(r==='DSM'){if(label)label.textContent='Code du PoS direct';if(id('childNode'))id('childNode').placeholder='Ex. POS1_DSM1_SU1 ou POS1';if(b){b.placeholder='Ex. POS1_DSM1_SU1 ou POS1';if(b.previousElementSibling)b.previousElementSibling.textContent='PoS direct';}if(a){a.placeholder='Ex. POS1_DSM1_SU1 ou POS1';if(a.previousElementSibling)a.previousElementSibling.textContent='PoS direct';}if(req)req.textContent='Votre supérieur direct est votre DAE. Son Robot approvisionnera votre SIM après les contrôles de solde et de taux.';}else if(r==='POS'){if(req)req.textContent='Votre supérieur direct est votre DSM. Son Robot approvisionnera votre SIM après les contrôles de solde et de taux.';}}
function hideDuplicates(){var q=document.querySelectorAll('[data-tab-panel="config"] button[data-native-action="permissions"],[data-tab-panel="config"] button[data-native-action="battery-settings"],[data-tab-panel="config"] button[data-native-action="start-robot"],[data-tab-panel="config"] button[data-native-action="verify-sim"]'),i;for(i=0;i<q.length;i++)q[i].style.display='none';}
function resultCard(){var flux=document.querySelector('[data-tab-panel="flux"]'),card=document.createElement('article');if(!flux)return;card.id='ctRemoteResult';card.className='panel bir-remote-result hidden';card.innerHTML='<p class="eyebrow">RETOUR CAMTEL DISTANT</p><h3>Résultat lisible sur ce Remote</h3><strong id="ctResultTitle">—</strong><div id="ctResultBody" class="result-body">Aucun résultat distant récent.</div>';flux.insertBefore(card,flux.firstChild);}
function commissionCard(c){var r=String(c.role||'').toUpperCase();if(r!=='DAE'&&r!=='DSM')return;var main=document.querySelector('main'),quick=document.querySelector('.bir-quick-controls'),card=document.createElement('article'),child=r==='DAE'?'DSM direct':'PoS direct';card.className='panel bir-commission-control';card.id='ctCommission';card.innerHTML='<p class="eyebrow">COMMISSIONS D\'APPROVISIONNEMENT</p><h3>Mes taux '+esc(child)+'</h3><p>Définissez le taux automatique, puis une exception seulement pour un enfant si nécessaire.</p><label>Taux par défaut (%)</label><div class="ct-row"><input id="ctDefaultRate" inputmode="decimal" placeholder="Ex. '+(r==='DAE'?'10':'8')+'"><button id="ctSaveDefault">ENREGISTRER</button></div><label>'+esc(child)+' et taux personnalisé</label><div class="ct-row"><input id="ctChild" placeholder="'+(r==='DAE'?'DSM1_SU1':'POS1_DSM1_SU1')+'"><input id="ctChildRate" inputmode="decimal" placeholder="Ex. 9,5"></div><div class="ct-row"><button id="ctSaveChild">PERSONNALISER</button><button id="ctResetChild">REVENIR AU DÉFAUT</button></div><div id="ctRateList" class="bir-rate-list">Lecture des taux…</div>';if(quick&&quick.parentNode)quick.parentNode.insertBefore(card,quick.nextSibling);else if(main)main.insertBefore(card,main.firstChild);
 id('ctSaveDefault').onclick=function(){platform('commission_set_default',{rate_bps:Math.round(Number(String(id('ctDefaultRate').value).replace(',','.'))*100)});};id('ctSaveChild').onclick=function(){platform('commission_set_child',{child_node_code:id('ctChild').value,rate_bps:Math.round(Number(String(id('ctChildRate').value).replace(',','.'))*100)});};id('ctResetChild').onclick=function(){platform('commission_set_child',{child_node_code:id('ctChild').value,use_default:true});};platform('commission_policy',{});}
function showResult(cmd){if(!cmd||cmd.state!=='SUCCEEDED'||!cmd.result_message)return;var op=String(cmd.operation||''),card=id('ctRemoteResult');if(!card)return;if(op==='BALANCE_CHILD'||op==='BALANCE_OWN'||op==='LAST_TRANSACTIONS'||op==='TRANSACTION_DETAILS'||op==='TEST_NUMBER'){card.className='panel bir-remote-result';id('ctResultTitle').textContent=(cmd.target_node_code?official(cmd.target_node_code,'', '')+' • ':'')+(op==='BALANCE_CHILD'?'SOLDE ENFANT':'PREUVE CAMTEL');id('ctResultBody').textContent=cmd.result_message;}}
function wrapCallbacks(c){window.BlueMagicNative=window.BlueMagicNative||{};var oldStatus=window.BlueMagicNative.onCommandStatus,oldPlatform=window.BlueMagicNative.onPlatformAction;window.BlueMagicNative.onCommandStatus=function(data){if(typeof oldStatus==='function')oldStatus(data);if(data&&data.command)showResult(data.command);};window.BlueMagicNative.onPlatformAction=function(data){if(typeof oldPlatform==='function')oldPlatform(data);if(!data||data.error)return;var a=data._action||'';if(a==='commission_policy'||a==='commission_set_default'||a==='commission_set_child'){if(id('ctDefaultRate'))id('ctDefaultRate').value=String(Number(data.default_rate_bps||0)/100).replace('.',',');var children=data.children||[],out=[],i;for(i=0;i<children.length;i++){out.push(esc(children[i].node_code)+' : <b>'+String(Number(children[i].effective_rate_bps||0)/100).replace('.',',')+' %</b> — '+(children[i].source==='CHILD_OVERRIDE'?'personnalisé':'défaut'));}if(id('ctRateList'))id('ctRateList').innerHTML=out.join('<br>')||'Aucun enfant direct enregistré.';}};}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
}());
''',encoding='utf-8')

# New contract tests are source-level; full historical suite remains mandatory in workflow.
Path('app/src/test/v2610-control-tower.mjs').write_text(r'''import fs from 'node:fs';
const html=fs.readFileSync('app/src/main/assets/index.html','utf8');
const js=fs.readFileSync('app/src/main/assets/control-tower-v2610.js','utf8');
const css=fs.readFileSync('app/src/main/assets/control-tower-v2610.css','utf8');
const app=fs.readFileSync('app/src/main/assets/app.js','utf8');
const main=fs.readFileSync('app/src/main/java/com/profitloop/blueauto/MainActivity.java','utf8');
const robot=fs.readFileSync('app/src/main/java/com/profitloop/blueauto/RobotService.java','utf8');
const worker=fs.readFileSync('cloudflare/src/index.js','utf8');
if(!html.includes('control-tower-v2610.js')||!html.includes('control-tower-v2610.css')) throw new Error('control tower not loaded');
if(css.includes('.form-editing .module-tabs{display:none}')) throw new Error('dock still hidden while editing');
for(const x of ['AUTORISATIONS','SYNCHRONISER','commission_set_default','commission_set_child','RETOUR CAMTEL DISTANT','DSM direct','PoS direct']) if(!js.includes(x)) throw new Error('missing UI contract '+x);
if(!app.includes("pendingPreview.type==='SUPPLY_CHILD'")||!app.includes('VOUS ÊTES LE SUPÉRIEUR')||!app.includes('Le taux est fixé par votre supérieur')) throw new Error('commission direction wording missing');
if(!main.includes('previewCommandWithCommissionRate')||!main.includes('createCommandWithCommissionRate')||!main.includes('synchronizeNow')) throw new Error('native bridge v2610 missing');
if(!robot.includes('ACTION_FORCE_SYNC')||!robot.includes('retryDueFinalReports(4)')) throw new Error('multi-profile sync recovery missing');
if(!worker.includes("source: 'TRANSACTION_OVERRIDE'")||!worker.includes("const API_VERSION = '2.6.10-cloudflare'")) throw new Error('worker one-off commission missing');
console.log('BIR v2.6.10 control tower: persistent dock, role hierarchy, commission direction, remote evidence and sync OK');
''',encoding='utf-8')
