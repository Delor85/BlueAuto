{
  "name": "blue-magic-cloudflare-api",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node test/integration.mjs",
    "dev": "wrangler dev",
    "db:local": "wrangler d1 migrations apply blue-magic --local",
    "db:remote": "wrangler d1 migrations apply blue-magic --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
